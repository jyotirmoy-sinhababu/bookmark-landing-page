import React from 'react';

import bookmark from '../../assest/img/logo-bookmark.svg';

const Logo = () => {
  return (
    <div>
      <img src={bookmark} alt='' />
    </div>
  );
};

export default Logo;
